
    function reduce(arr, fn, initial) {
        let res = arr.reduce( (accumulator, currentValue) => {

        }, initial);
        return res
    }

    module.exports = reduce


// ??????????????????????????